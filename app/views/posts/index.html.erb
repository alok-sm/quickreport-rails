<h1>Listing posts</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Description</th>
    <th>Lattitude</th>
    <th>Longitude</th>
    <th>Category</th>
    <th>Hashtag</th>
    <th>State Code</th>
    <th>State</th>
    <th>Twitter pic url </th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.id %></td>
    <td><%= post.description %></td>
    <td><%= post.lattitude %></td>
    <td><%= post.longitude %></td>
    <td><%= post.category %></td>
    <td><%= post.hashtag %></td>
    <td><%= post.statecode %></td>
    <td><%= post.state %></td>
    <td><%= link_to(post.tpicurl, post.tpicurl) %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
